{
  "scripts": {
    "frontend": "cd frontend && yarn install && yarn build",
    "backend": "cd backend && yarn prestart && yarn start",
    "heroku-postbuild": "yarn backend && yarn frontend"
  }
}
