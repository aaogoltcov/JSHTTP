{
  "name": "JSHTTP",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/aaogoltcov/JSHTTP.git",
  "author": "aaogoltcov",
  "license": "MIT",
  "scripts": {
    "frontend": "cd frontend && yarn install && yarn start",
    "backend": "cd backend && yarn prestart && yarn start",
    "dev": "concurrently --kill-others-on-fail \"yarn backend\" \"yarn frontend\"",
    "heroku-postbuild": "yarn frontend && yarn install && yarn build"
  },
  "dependencies": {
    "yarn": "^1.22.10"
  }
}
